<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Monthly Sprint Report</title>
  <style>
    body {
      font-family: Arial, Helvetica, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f5f6fa;
      color: #333;
    }
    .email-wrapper {
      max-width: 700px;
      margin: 0 auto;
      background: #ffffff;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }
    .header {
      background: linear-gradient(135deg, #2c3e50, #4ca1af);
      color: #fff;
      padding: 20px 24px;
      text-align: center;
    }
    .header h1 {
      margin: 0;
      font-size: 24px;
    }
    .header p {
      margin: 6px 0 0;
      font-size: 14px;
      opacity: 0.85;
    }
    .content {
      padding: 24px;
    }
    .section-title {
      font-size: 18px;
      margin-bottom: 10px;
      color: #2c3e50;
      border-bottom: 2px solid #eee;
      padding-bottom: 4px;
    }
    table.metrics {
      width: 100%;
      border-collapse: collapse;
      margin-top: 12px;
    }
    table.metrics th,
    table.metrics td {
      padding: 10px;
      border: 1px solid #ddd;
      text-align: left;
      font-size: 14px;
    }
    table.metrics th {
      background: #f9f9f9;
      color: #555;
    }
    .no-data {
      padding: 16px;
      background: #fef4f4;
      border: 1px solid #f8d7da;
      color: #721c24;
      border-radius: 4px;
      margin-top: 10px;
      font-size: 14px;
    }
    .footer {
      background: #f5f6fa;
      padding: 18px 24px;
      text-align: center;
      font-size: 13px;
      color: #888;
    }
    .footer a {
      color: #4ca1af;
      text-decoration: none;
    }
    @media only screen and (max-width: 600px) {
      .header h1 {
        font-size: 20px;
      }
      table.metrics th,
      table.metrics td {
        font-size: 13px;
        padding: 8px;
      }
    }
  </style>
</head>
<body>
  <div class="email-wrapper">
    <div class="header">
      <h1>Monthly Sprint Report</h1>
      <p><%= @project.name %> — <%= @summary[:period_start].strftime('%d %b %Y') %> to <%= @summary[:period_end].strftime('%d %b %Y') %></p>
    </div>

    <div class="content">
      <div class="section-title">Summary</div>
      <% if @summary.present? %>
        <table class="metrics">
          <thead>
            <tr>
              <th>Metric</th>
              <th>Value</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>Total Sprints in Period</td>
              <td><%= @summary[:sprints] %></td>
            </tr>
            <tr>
              <td>Completed Sprints</td>
              <td><%= @summary[:completed_sprints] %></td>
            </tr>
            <tr>
              <td>Open Sprints</td>
              <td><%= @summary[:open_sprints] %></td>
            </tr>
            <tr>
              <td>Issues Resolved</td>
              <td><%= @summary[:issues_resolved] %></td>
            </tr>
            <tr>
              <td>Closed Issues in Period</td>
              <td><%= @summary[:closed_issues] %></td>
            </tr>
            <tr>
              <td>Story Points Completed</td>
              <td><%= @summary[:story_points_done] %></td>
            </tr>
          </tbody>
        </table>
      <% else %>
        <div class="no-data">
          No sprints completed during this reporting period.
        </div>
      <% end %>

      <p style="margin-top: 20px; font-size: 14px;">
        📊 For detailed burndown charts, velocity trends, and backlog insights,  
        visit the <%= link_to 'Project Dashboard', project_url(@project) %>.
      </p>
    </div>

    <div class="footer">
      You are receiving this report because you are subscribed to sprint updates for <strong><%= @project.name %></strong>.  
      <br>
      <a href="<%= project_url(@project) %>">Manage notification settings</a>
    </div>
  </div>
</body>
</html>